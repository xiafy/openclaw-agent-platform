# {{ agent_name }} - Deployment Record

> **Deployed:** {{ deploy_time }}
> **Mode:** {{ mode.upper() }}
> **Version:** deploy-agent v1.0

---

## Configuration

| Item | Value |
|------|-------|
| **Name** | {{ agent_name }} |
| **Role** | {{ role }} |
| **Mode** | {{ mode }} |
{% if mode == 'l2' %}
| **Username** | {{ username }} |
| **UID** | {{ uid }} |
{% endif %}
| **Port** | {{ port }} |
| **Bot** | @{{ bot_username }} |

---

## Deployment Steps

{% for step in steps %}
{{ loop.index }}. {{ step }}
{% endfor %}

---

## Verification

{% for check in verification_checks %}
- [ ] {{ check }}
{% endfor %}

---

## Post-Deployment Tasks

- [ ] Telegram pairing
- [ ] Test basic commands
- [ ] Configure skills (if needed)
- [ ] Set up cron jobs (if needed)

---

## Troubleshooting

### Gateway not starting
```bash
# Check logs
tail -f /tmp/openclaw-{{ username if mode == 'l2' else agent_name }}/openclaw.log

# Check process
ps aux | grep openclaw

# Check port
lsof -i :{{ port }}
```

### Telegram not responding
```bash
# Check bot token
# Restart gateway
```

---

*Generated by deploy-agent v1.0*
